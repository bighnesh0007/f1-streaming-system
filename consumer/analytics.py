import random

def generate_analytics(driver_number):
    avg_speed = random.uniform(200, 300)
    max_speed = random.uniform(300, 350)
    prediction_score = random.uniform(0.5, 1.0)

    return avg_speed, max_speed, prediction_score